# World Builder settings (bin64/cdmodkit/settings.txt)
# key_toggle: show / hide the editor    key_mode: edit mode / play mode
# Names: INSERT HOME END DELETE PAGEUP PAGEDOWN F1..F12 SCROLLLOCK PAUSE BACKQUOTE MINUS EQUALS BACKSLASH NUMPAD* NUMPAD/ NUMLOCK CAPSLOCK TAB or a single letter/digit
key_toggle=INSERT
key_mode=HOME
# console=1 opens a console window next to the game with the log (the log file is always written)
console=0
# HTTP API for programs on this PC (127.0.0.1 only, see HTTP_API.md): off by default, http_api=1 or the Settings tab checkbox turns it on
http_api=0
http_port=8765
# gizmo projection: fovauto=1 reads the field of view from the game camera; fov= is the manual fallback (degrees), mirror=1 flips horizontally
fovauto=1
fov=55.0
mirror=0
